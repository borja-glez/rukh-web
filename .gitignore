node_modules/
dist/
# cm-chessboard sprites copied by scripts/copy-assets.mjs (predev/prebuild)
/public/pieces/
/public/extensions/
# onnxruntime-web runtime copied by scripts/copy-assets.mjs (predev/prebuild)
/public/ort/
.astro/
.lighthouseci/
lighthouse-reports/
playwright-report/
test-results/
blob-report/
.env
.env.*
*.log
.DS_Store
Thumbs.db

# Stray sidecar the dynamo ONNX exporter leaves behind; `set_metadata` re-saves the weights
# inline, so the `.onnx` is self-contained and the `.data` is dead weight.
public/test/*.onnx.data
