// Syncs the UCI tokenizer vocabulary and parity fixtures from the Hugging Face Hub
// (chorcat/rukh-tokenizer) into src/lib/chess-lm/. The tokenizer is produced in P1 by the
// ML repo; until it is published there is nothing to sync.
console.log(
  'sync:tokenizer: the tokenizer arrives in P1 (chorcat/rukh-tokenizer); nothing to sync yet.',
);
process.exit(0);
